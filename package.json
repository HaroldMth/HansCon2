{
  "name": "your-project",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p public && cp -r * public", 
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.5.4"
  }
}
